# Usage
```
$ git clone https://github.com/s7electric/svd_compression_py
$ cd svd_compression_py/
$ python3 svd_compress.py
```

This will create a number of images, each with varying levels of compression.
The "phaseX-RECOMBINED.jpg" are the final images for each compression level; other images are just the decomposed colour channels.
